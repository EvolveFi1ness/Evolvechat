# Secrets — never commit
.env
*.env
!/.env.example

# Owner-only dashboard — local machine, never pushed
monitoring.html

# Dependencies (monitoring worker)
node_modules/
scripts/monitor/node_modules/

# OS / editor noise
Thumbs.db
.DS_Store
