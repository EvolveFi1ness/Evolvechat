# Secrets — never commit
.env
*.env
!/.env.example

# Dependencies (monitoring worker)
node_modules/
scripts/monitor/node_modules/

# OS / editor noise
Thumbs.db
.DS_Store

# Firebase CLI noise
firebase-debug.log
